#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int num;
while (num < 10){
printf("%d", num);
num++;
}
putchar('\n');
return (0);
}
